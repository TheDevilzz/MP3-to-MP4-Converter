{
  "name": "mp3-to-mp4-youtube-workbench",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack MP3 to MP4 converter with SSE progress and YouTube upload integration.",
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "lint": "npm --prefix client run lint",
    "check:server": "node --check server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
